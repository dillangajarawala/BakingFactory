<% if current_user.role?(:admin) %>
    <%= form_with url: search_path, method: :get, class: "form-inline", id: "search" do %>
                <%= text_field_tag 'query', '', placeholder: "Search Items and Customers..." %>
                <!-- submitting via js (press enter) so not using... -->
                <%# submit_tag('Find') %>
                
            <% end %>
<% elsif current_user.role?(:customer) %>
    <%= form_with url: search_path, method: :get, class: "form-inline", id: "search2" do %>
                <%= text_field_tag 'query', '', placeholder: "Search ..." %>
                <!-- submitting via js (press enter) so not using... -->
                <%# submit_tag('Find') %>
                
            <% end %>
<% end %>