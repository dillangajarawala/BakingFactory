<% if logged_in? && current_user.role?(:admin) %>
  <%= render partial: "partials/structure1", locals: {primary: "item_details", 
                                                    secondary: nil, sidebar: "partials/price_history" }%>

<% else %>
  <%= render partial: "partials/structure1", locals: {primary: "item_details", 
                                                    secondary: nil, sidebar: "partials/similar_items" }%>

<% end %>

<!-- Add buttons to list all, edit project or destroy object -->
<%= render partial: "partials/object_options", locals: {model_name: "item", object: @item}  %>